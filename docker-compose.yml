services:
    frontend:
        build: .
        ports:
            - '3000:3000'
        env_file:
            - .env.local
        environment:
            - WATCHPACK_POLLING=true
        volumes:
            - .:/app
            - /app/node_modules
        restart: unless-stopped
